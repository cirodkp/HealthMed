apiVersion: v1
kind: Service
metadata:
  name: infra-consumer-doctor-service
  namespace: healthmed
spec:
  selector:
    app: infra-consumer-doctor
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort