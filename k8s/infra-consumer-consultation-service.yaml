apiVersion: v1
kind: Service
metadata:
  name: infra-consumer-consultation-service
  namespace: healthmed
spec:
  selector:
    app: infra-consumer-consultation
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort