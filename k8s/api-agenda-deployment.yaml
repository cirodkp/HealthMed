apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-agenda-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-agenda
  template:
    metadata:
      labels:
        app: api-agenda
    spec:
      containers:
        - name: apiage
          image: ciroa/health-med:HealthMed.Agenda.API-latest
          ports:
            - containerPort: 8080
          env:
            - name: ConnectionStrings__postgres
              value: "Server=postgres-service;Port=5432;Database=HealthMed;User Id=admin;Password=$(POSTGRES_PASSWORD);"
          envFrom:
            - configMapRef:
                name: api-agenda-config
            - secretRef:
                name: postgres-secret
